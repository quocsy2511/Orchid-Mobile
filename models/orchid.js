class Orchid {
    constructor(
        id,
        categoryIds,
        name,
        description,
        imageUrl,
        rating,
        price,
    ) {
        this.id = id
        this.categoryIds = categoryIds
        this.name = name
        this.description = description
        this.imageUrl = imageUrl
        this.rating = rating
        this.price = price
    }
}

export default Orchid;